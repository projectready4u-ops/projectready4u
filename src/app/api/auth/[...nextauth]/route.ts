// NextAuth route placeholder
// Future implementation for OAuth login

export const GET = async () => {
  return new Response('OAuth not configured yet', { status: 501 });
};

export const POST = async () => {
  return new Response('OAuth not configured yet', { status: 501 });
};
